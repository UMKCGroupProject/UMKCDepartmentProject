# Copy to .env. Only VITE_-prefixed variables reach the client bundle.

# Left as /api in development so requests go through the Vite dev proxy
# (see vite.config.js). Point it at the API's public URL in production.
VITE_API_URL=/api
